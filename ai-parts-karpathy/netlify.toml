[build]
  command = "cd demo-server && npm install && npm run build"
  publish = "demo-server/build"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/render"
  status = 200
